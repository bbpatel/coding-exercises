you need to install style and css loaders to work with webpack. Webpack only handles javascript natively so for css or styles you need to run this in project directory
and not with -g or global options. has to be done in project directory 
-------------FOLLOWING MUST BE RUN FROM PROJECT DIR FOR STYLES AND CSS TO WORK ---------------------------
npm install css-loader style-loader